{
    "builds": [{
        "src": "EquipSenseWebApp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "EquipSenseWebApp/wsgi.py"
        }
    ]
}